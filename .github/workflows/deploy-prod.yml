# =============================================================================
# Alpla Angola - Portal Gerencial
# GitHub Actions Workflow: Deploy to PRODUCTION Environment
# =============================================================================
#
# Target Server:      AOVIA1VMS011
# Target Environment: PRODUCTION
# Official URL:       https://portalgerencial.alpla.net/
# API Port:           5002 (NOT 5000 or 5001)
#
# This workflow builds the backend (.NET 8) and frontend (React/Vite),
# then deploys both to the PRODUCTION environment on the self-hosted runner.
#
# IMPORTANT:
# - This workflow does NOT touch the Test environment.
# - This workflow does NOT touch port 5001 or Test App Pools.
# - This workflow creates a SQL backup before deployment.
# - This workflow validates the target database is [Portal-Gerencial].
# - Migrations are NOT automated — the workflow checks for pending
#   migrations and blocks startup if any are found (DEC-137).
#
# SAFETY:
# - Requires GitHub environment 'production' with approval gates.
# - Separate concurrency group from Test deployments.
# - Database name validation prevents accidental Test DB modifications.
# =============================================================================

name: Deploy to PRODUCTION

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g. v2.185.0)'
        required: true
        type: string

# Prevent concurrent deployments to PRODUCTION
concurrency:
  group: deploy-prod
  cancel-in-progress: false

jobs:
  # ===========================================================================
  # Job 1: Build
  # ===========================================================================
  # Compiles backend and frontend on a GitHub-hosted runner, then uploads
  # the artifacts for the deploy job.
  # ===========================================================================
  build:
    name: Build (${{ inputs.version }})
    runs-on: windows-latest
    timeout-minutes: 20

    outputs:
      build_id: ${{ steps.meta.outputs.build_id }}
      version: ${{ steps.meta.outputs.version }}
      short_sha: ${{ steps.meta.outputs.short_sha }}

    steps:
      # --- Checkout ---
      - name: Checkout repository
        uses: actions/checkout@v4

      # --- Validate version consistency & compute canonical build metadata ---
      # Authoritative version = docs/CHANGELOG.md ("Current Version"); VERSION.md + config.ts must
      # agree; the manual input is a CONFIRMATION only and must equal it. Any inconsistency fails the
      # build (never silently corrected). buildId = <version>+<shortSha> (environment-independent).
      - name: Validate version and compute build metadata
        id: meta
        shell: pwsh
        run: |
          function Get-CurrentVersion($text) {
            if ($text -match '(?ms)Current Version\s*\r?\n\s*\r?\n\s*v?([0-9]+\.[0-9]+\.[0-9]+)') { return $Matches[1] }
            return $null
          }
          $changelog = Get-Content docs/CHANGELOG.md -Raw
          $versionMd = Get-Content docs/VERSION.md -Raw
          $configTs  = Get-Content src/frontend/src/config.ts -Raw

          $chVer = Get-CurrentVersion $changelog
          $vmVer = Get-CurrentVersion $versionMd
          $cfgVer = $null
          if ($configTs -match "APP_VERSION\s*=\s*'v?([0-9]+\.[0-9]+\.[0-9]+)'") { $cfgVer = $Matches[1] }

          $inputVer = "${{ inputs.version }}".Trim().TrimStart('v')

          if ([string]::IsNullOrWhiteSpace($chVer)) { throw "Could not read 'Current Version' from docs/CHANGELOG.md" }
          if ($chVer -ne $vmVer)  { throw "Version inconsistency: CHANGELOG=$chVer but VERSION.md=$vmVer" }
          if ($chVer -ne $cfgVer) { throw "Version inconsistency: CHANGELOG=$chVer but config.ts=$cfgVer" }
          if ([string]::IsNullOrWhiteSpace($inputVer)) { throw "Workflow version input is required." }
          if ($inputVer -ne $chVer) { throw "Workflow input ($inputVer) does not match repository version ($chVer). Aborting." }

          $shortSha = "${{ github.sha }}".Substring(0,7)
          $buildId  = "$chVer+$shortSha"
          $builtAt  = [DateTime]::UtcNow.ToString("o")

          Write-Host "::notice::Validated version $chVer; buildId=$buildId"
          "version=$chVer"      | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "short_sha=$shortSha" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "build_id=$buildId"   | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "built_at=$builtAt"   | Out-File -FilePath $env:GITHUB_OUTPUT -Append

      # --- Backend (.NET 8) ---
      - name: Setup .NET 8 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore backend dependencies
        run: dotnet restore src/backend/AlplaPortal.sln

      - name: Build backend (Release)
        run: dotnet build src/backend/AlplaPortal.sln --configuration Release --no-restore

      - name: Publish backend API
        run: >
          dotnet publish src/backend/AlplaPortal.Api/AlplaPortal.Api.csproj
          --configuration Release
          --no-build
          --output ${{ github.workspace }}/artifacts/api

      # --- Frontend (React + Vite) ---
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: src/frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: src/frontend
        run: npm ci

      - name: Frontend type check
        working-directory: src/frontend
        run: npx tsc --noEmit

      - name: Build frontend
        working-directory: src/frontend
        env:
          # Compile the canonical build identity into the bundle (public, non-secret). NEVER put
          # secrets in VITE_* — they are visible in the browser.
          VITE_APP_VERSION: ${{ steps.meta.outputs.version }}
          VITE_APP_BUILD_ID: ${{ steps.meta.outputs.build_id }}
          VITE_APP_SHORT_SHA: ${{ steps.meta.outputs.short_sha }}
          VITE_APP_BUILT_AT: ${{ steps.meta.outputs.built_at }}
        run: npm run build

      - name: Copy frontend dist to artifact folder
        shell: pwsh
        run: |
          $source = "src/frontend/dist"
          $destination = "${{ github.workspace }}/artifacts/web"
          New-Item -ItemType Directory -Force -Path $destination | Out-Null
          robocopy $source $destination /E /NFL /NDL /NJH /NJS /NC /NS /NP
          if ($LASTEXITCODE -gt 7) { throw "Frontend dist copy failed with robocopy exit code $LASTEXITCODE" }
          $global:LASTEXITCODE = 0

      - name: Validate frontend artifact structure
        shell: pwsh
        run: |
          $assetsDir = "${{ github.workspace }}/artifacts/web/assets"
          if (-not (Test-Path $assetsDir)) {
            throw "FATAL: artifacts/web/assets/ directory is missing. Vite build output was not preserved correctly."
          }
          $jsFiles = Get-ChildItem -Path $assetsDir -Filter "*.js" -ErrorAction SilentlyContinue
          $cssFiles = Get-ChildItem -Path $assetsDir -Filter "*.css" -ErrorAction SilentlyContinue
          if (-not $jsFiles) { throw "FATAL: No .js files found in artifacts/web/assets/" }
          if (-not $cssFiles) { throw "FATAL: No .css files found in artifacts/web/assets/" }
          Write-Host "Frontend artifact structure validated:"
          Write-Host "  JS files: $($jsFiles.Count)"
          Write-Host "  CSS files: $($cssFiles.Count)"

      # --- Build manifest + public frontend metadata (canonical build identity) ---
      - name: Generate build manifest and frontend metadata
        shell: pwsh
        run: |
          $manifest = [ordered]@{
            version    = "${{ steps.meta.outputs.version }}"
            gitSha     = "${{ github.sha }}"
            shortSha   = "${{ steps.meta.outputs.short_sha }}"
            buildId    = "${{ steps.meta.outputs.build_id }}"
            builtAtUtc = "${{ steps.meta.outputs.built_at }}"
          }
          $json = $manifest | ConvertTo-Json -Depth 5
          $json | Set-Content -Path "${{ github.workspace }}/artifacts/api/build-manifest.json" -Encoding UTF8
          $json | Set-Content -Path "${{ github.workspace }}/artifacts/web/build-manifest.json" -Encoding UTF8
          $json | Set-Content -Path "${{ github.workspace }}/artifacts/web/build-metadata.json" -Encoding UTF8
          Write-Host "::notice::Build manifest written to both artifacts (buildId=${{ steps.meta.outputs.build_id }})"

      # --- Generate Expected Migration List (DEC-139) ---
      - name: Generate expected migration list
        shell: pwsh
        run: |
          $migrationsPath = "src/backend/AlplaPortal.Infrastructure/Data/Migrations"
          $outputFile = "${{ github.workspace }}/artifacts/api/expected-migrations.txt"
          $migrations = Get-ChildItem -Path $migrationsPath -Filter "*.cs" -Name |
            Where-Object { $_ -notmatch '\.Designer\.cs$' -and $_ -notmatch 'Snapshot\.cs$' } |
            ForEach-Object { $_ -replace '\.cs$', '' } |
            Sort-Object
          $migrations | Set-Content -Path $outputFile -Encoding UTF8
          Write-Host "Generated expected-migrations.txt with $($migrations.Count) migrations"

      # --- Artifact integrity: canonical inventory + aggregate (SHA-256) ---
      # Writes a PERSISTED, line-level canonical inventory (artifact-inventory.sha256) into EACH artifact,
      # plus an aggregate (artifact-sha256.txt) = SHA-256 of that inventory file's exact UTF-8 (no BOM)
      # bytes. Ordinal sort only (culture-independent). The self-hosted runner re-derives the inventory
      # from the downloaded files and compares it LINE BY LINE, so any content/set drift is provable — not
      # an opaque hash mismatch. Chain: files -> artifact-inventory.sha256 -> artifact-sha256.txt. The
      # inventory excludes the two integrity files AND all hidden paths so it matches
      # actions/upload-artifact@v4 default inclusion (hidden files like .gitkeep are NOT uploaded).
      # SEPARATE control from manifest-equality (binary integrity vs build identity).
      - name: Compute artifact inventory and SHA-256
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          # Canonical inventory helpers (identical on build and deploy sides): one "<relpath> <sha256>"
          # line per file, lowercased forward-slash paths, ORDINAL sort. Excludes the two integrity files
          # AND all hidden paths (dot-prefixed filename OR any dot-prefixed directory segment) so the set
          # matches actions/upload-artifact@v4 default inclusion (hidden files like .gitkeep are not
          # uploaded). Only a path SEGMENT whose first char is '.' is hidden — dots elsewhere are kept.
          function Test-IsHiddenArtifactPath {
            param([string]$RelativePath)
            foreach ($segment in ($RelativePath -split '/')) {
              if ($segment.StartsWith('.', [System.StringComparison]::Ordinal)) { return $true }
            }
            return $false
          }
          function Get-CanonicalInventory($path) {
            $root = (Resolve-Path -LiteralPath $path).Path.TrimEnd('\')
            $included = New-Object System.Collections.Generic.List[string]
            $excluded = New-Object System.Collections.Generic.List[string]
            foreach ($f in (Get-ChildItem -LiteralPath $root -Recurse -File)) {
              if ($f.Name -ieq 'artifact-sha256.txt' -or $f.Name -ieq 'artifact-inventory.sha256') { continue }
              $rel = $f.FullName.Substring($root.Length).TrimStart('\').Replace('\','/').ToLowerInvariant()
              if (Test-IsHiddenArtifactPath $rel) { $excluded.Add($rel); continue }
              $h = (Get-FileHash -LiteralPath $f.FullName -Algorithm SHA256).Hash.ToLowerInvariant()
              $included.Add("$rel $h")
            }
            $lines = [string[]]$included.ToArray()
            [Array]::Sort($lines, [System.StringComparer]::Ordinal)
            $hidden = [string[]]$excluded.ToArray()
            [Array]::Sort($hidden, [System.StringComparer]::Ordinal)
            return [pscustomobject]@{ Lines = $lines; Excluded = $hidden }
          }
          function Write-Utf8NoBom($filePath, $text) {
            $enc = New-Object System.Text.UTF8Encoding($false)
            [System.IO.File]::WriteAllText($filePath, $text, $enc)
          }
          foreach ($a in @(
            @{ Path = "${{ github.workspace }}/artifacts/api"; Label = 'api' },
            @{ Path = "${{ github.workspace }}/artifacts/web"; Label = 'web' }
          )) {
            $result = Get-CanonicalInventory $a.Path
            $inv = $result.Lines
            $joined = ($inv -join "`n")
            $invFile = Join-Path $a.Path 'artifact-inventory.sha256'
            Write-Utf8NoBom $invFile $joined
            # Aggregate is the SHA-256 of the EXACT bytes just written (no independently rebuilt string).
            $agg = (Get-FileHash -LiteralPath $invFile -Algorithm SHA256).Hash.ToLowerInvariant()
            Write-Utf8NoBom (Join-Path $a.Path 'artifact-sha256.txt') $agg
            Write-Host "::notice::$($a.Label) inventory: includedFiles=$($inv.Count) excludedHiddenFiles=$($result.Excluded.Count)"
            Write-Host "::notice::$($a.Label)ArtifactSha256=$agg"
            if ($result.Excluded.Count -gt 0) {
              Write-Host "$($a.Label) excluded hidden paths (capped at 20; not uploaded by actions/upload-artifact):"
              $result.Excluded | Select-Object -First 20 | ForEach-Object { Write-Host "  EXCLUDED-HIDDEN: $_" }
            }
          }

      # --- Upload Artifacts ---
      - name: Upload API artifact
        uses: actions/upload-artifact@v4
        with:
          name: api-artifact-prod
          path: ${{ github.workspace }}/artifacts/api
          retention-days: 14
          if-no-files-found: error

      - name: Upload Web artifact
        uses: actions/upload-artifact@v4
        with:
          name: web-artifact-prod
          path: ${{ github.workspace }}/artifacts/web
          retention-days: 14
          if-no-files-found: error

  # ===========================================================================
  # Job 2: Deploy to PRODUCTION
  # ===========================================================================
  # Downloads artifacts on the self-hosted runner, validates the database,
  # creates backups, stops IIS, deploys new files, starts IIS, and runs
  # a smoke test.
  #
  # SAFETY:
  # - Database name validation (must be [Portal-Gerencial], NOT [Portal-Gerencial-Test])
  # - SQL backup before deployment
  # - Only touches PRODUCTION App Pools (never Test)
  # - GitHub environment approval gate
  # ===========================================================================
  deploy-prod:
    name: Deploy PRODUCTION (${{ inputs.version }})
    needs: build
    runs-on: [self-hosted, Windows, X64, iis]
    # 45 min to match TEST: the self-hosted runner's egress to the artifact backend is slow
    # (~0.6 Mbit/s measured) and the api-artifact is ~121 MB, which can exceed a 20 min budget.
    timeout-minutes: 45
    environment: production

    env:
      ARTIFACT_API_PATH: C:\ActionsArtifacts\AlplaPortal\prod\api
      ARTIFACT_WEB_PATH: C:\ActionsArtifacts\AlplaPortal\prod\web

    steps:
      # --- Fail-fast: required deployment variables (before ANY server-touching work) ---
      - name: Validate deployment variables
        shell: pwsh
        run: |
          $portal = "${{ vars.PROD_PORTAL_URL }}"
          if ([string]::IsNullOrWhiteSpace($portal)) { throw "Repository variable PROD_PORTAL_URL is not set (expected https://portalgerencial.alpla.net)." }
          if ($portal -notmatch '^https://') { throw "PROD_PORTAL_URL must be an https:// URL. Got: $portal" }
          if ($portal -match '(?i)portalgerencial-test\.alpla\.net') { throw "PROD_PORTAL_URL points to the TEST host — aborting to prevent a cross-environment deploy." }
          if ($portal -notmatch '(?i)^https://portalgerencial\.alpla\.net') { throw "PROD_PORTAL_URL does not match the PRODUCTION host (expected https://portalgerencial.alpla.net). Got: $portal" }
          Write-Host "::notice::PROD_PORTAL_URL validated: $portal"

      # --- Download Artifacts ---
      - name: Clean artifact staging directories
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          # Static per-environment staging path is RETAINED (not run_id-scoped): the strict clean below
          # already guarantees an empty tree each run, whereas run_id-scoped folders would accumulate on
          # the self-hosted runner with no garbage collection. Fail-closed: any partial delete aborts the
          # job BEFORE download, so a stale/locked leftover file can never corrupt integrity verification.
          $paths = @("${{ env.ARTIFACT_API_PATH }}", "${{ env.ARTIFACT_WEB_PATH }}")
          foreach ($p in $paths) {
            if (Test-Path -LiteralPath $p) {
              Remove-Item -LiteralPath $p -Recurse -Force -ErrorAction Stop
            }
            if (Test-Path -LiteralPath $p) { throw "Staging cleanup FAILED: '$p' still exists after removal. Aborting (a stale/locked file could corrupt integrity verification)." }
          }
          foreach ($p in $paths) {
            New-Item -ItemType Directory -Force -Path $p -ErrorAction Stop | Out-Null
            if (-not (Test-Path -LiteralPath $p)) { throw "Staging cleanup FAILED: could not create '$p'." }
            $remaining = @(Get-ChildItem -LiteralPath $p -Force -ErrorAction SilentlyContinue)
            if ($remaining.Count -gt 0) { throw "Staging cleanup FAILED: '$p' is not empty after recreation ($($remaining.Count) item(s))." }
          }
          Write-Host "::notice::Staging directories cleaned and verified empty (api + web)."

      - name: Download API artifact
        uses: actions/download-artifact@v4
        with:
          name: api-artifact-prod
          path: ${{ env.ARTIFACT_API_PATH }}

      - name: Download Web artifact
        uses: actions/download-artifact@v4
        with:
          name: web-artifact-prod
          path: ${{ env.ARTIFACT_WEB_PATH }}

      # --- GATE: build identity (FE == BE == expected) — before any server mutation ---
      - name: Verify build manifests (frontend == backend == expected)
        shell: pwsh
        run: |
          $expected = "${{ needs.build.outputs.build_id }}"
          $apiManifest = Join-Path "${{ env.ARTIFACT_API_PATH }}" "build-manifest.json"
          $webManifest = Join-Path "${{ env.ARTIFACT_WEB_PATH }}" "build-manifest.json"
          foreach ($m in @($apiManifest, $webManifest)) {
            if (-not (Test-Path $m)) { throw "FATAL: build manifest missing ($m). Deployment metadata invalid — aborting fail-closed." }
          }
          $api = Get-Content $apiManifest -Raw | ConvertFrom-Json
          $web = Get-Content $webManifest -Raw | ConvertFrom-Json
          if ([string]::IsNullOrWhiteSpace($api.buildId)) { throw "FATAL: API manifest has no buildId (malformed)." }
          if ($api.buildId -ne $web.buildId) { throw "FATAL: build identity mismatch api=$($api.buildId) web=$($web.buildId). Aborting before touching PRODUCTION." }
          if ($expected -and $api.buildId -ne $expected) { throw "FATAL: artifact buildId ($($api.buildId)) != expected ($expected). Aborting." }
          Write-Host "::notice::Build identity verified: $($api.buildId)"

      # --- GATE: artifact integrity (inventory + SHA-256) — before any server mutation ---
      # Two independent checks per artifact, BOTH fail-closed before any server change:
      #   (1) aggregate: SHA-256 of the downloaded artifact-inventory.sha256 file must equal the shipped
      #       artifact-sha256.txt (detects a corrupt/tampered inventory or aggregate);
      #   (2) content:  re-derive the canonical inventory from the downloaded files and diff it LINE BY
      #       LINE against the shipped inventory (detects any added/removed/changed file — the exact
      #       evidence the previous opaque folder-hash could not provide).
      # Runs BEFORE DB checks, backup, pool stop, file copy and web.config patch. Separate control from
      # the manifest-equality (identity) gate above.
      - name: Verify artifact inventory and SHA-256 (integrity)
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          # Same canonical inventory helpers as the build side (ordinal sort, culture-independent; excludes
          # the two integrity files AND all hidden paths so the recalculated set matches what
          # actions/upload-artifact@v4 actually uploaded — hidden files like .gitkeep are not transported).
          function Test-IsHiddenArtifactPath {
            param([string]$RelativePath)
            foreach ($segment in ($RelativePath -split '/')) {
              if ($segment.StartsWith('.', [System.StringComparison]::Ordinal)) { return $true }
            }
            return $false
          }
          function Get-CanonicalInventory($path) {
            $root = (Resolve-Path -LiteralPath $path).Path.TrimEnd('\')
            $included = New-Object System.Collections.Generic.List[string]
            $excluded = New-Object System.Collections.Generic.List[string]
            foreach ($f in (Get-ChildItem -LiteralPath $root -Recurse -File)) {
              if ($f.Name -ieq 'artifact-sha256.txt' -or $f.Name -ieq 'artifact-inventory.sha256') { continue }
              $rel = $f.FullName.Substring($root.Length).TrimStart('\').Replace('\','/').ToLowerInvariant()
              if (Test-IsHiddenArtifactPath $rel) { $excluded.Add($rel); continue }
              $h = (Get-FileHash -LiteralPath $f.FullName -Algorithm SHA256).Hash.ToLowerInvariant()
              $included.Add("$rel $h")
            }
            $lines = [string[]]$included.ToArray()
            [Array]::Sort($lines, [System.StringComparer]::Ordinal)
            $hidden = [string[]]$excluded.ToArray()
            [Array]::Sort($hidden, [System.StringComparer]::Ordinal)
            return [pscustomobject]@{ Lines = $lines; Excluded = $hidden }
          }
          function Get-InventoryLines($filePath) {
            $raw = [System.IO.File]::ReadAllText($filePath)
            if ([string]::IsNullOrEmpty($raw)) { return , @() }
            return , @(($raw -split "`n") | ForEach-Object { $_.TrimEnd("`r") } | Where-Object { $_ -ne '' })
          }
          function ConvertTo-InventoryMap($lines) {
            $map = @{}
            foreach ($ln in $lines) {
              $idx = $ln.LastIndexOf(' ')
              if ($idx -lt 1) { throw "Malformed inventory line (no separator): '$ln'" }
              $map[$ln.Substring(0, $idx)] = $ln.Substring($idx + 1)
            }
            return $map
          }
          function Confirm-Artifact($path, $label) {
            $invFile = Join-Path $path 'artifact-inventory.sha256'
            $aggFile = Join-Path $path 'artifact-sha256.txt'
            if (-not (Test-Path -LiteralPath $invFile)) { throw "artifact-inventory.sha256 missing in $label ($path) - cannot verify integrity." }
            if (-not (Test-Path -LiteralPath $aggFile)) { throw "artifact-sha256.txt missing in $label ($path) - cannot verify integrity." }

            # (1) Aggregate gate: shipped aggregate must equal SHA-256 of the shipped inventory FILE bytes.
            $expectedAgg = ([string][System.IO.File]::ReadAllText($aggFile)).Trim().ToLowerInvariant()
            $actualAgg = (Get-FileHash -LiteralPath $invFile -Algorithm SHA256).Hash.ToLowerInvariant()
            if ($expectedAgg -ne $actualAgg) {
              Write-Host "${label}: expectedAggregate=$expectedAgg actualInventoryFileAggregate=$actualAgg"
              throw "ARTIFACT INTEGRITY FAILURE ($label): inventory aggregate mismatch (artifact-inventory.sha256 does not match artifact-sha256.txt). Aborting before touching the server."
            }

            # (2) Content gate: re-derive inventory from downloaded files, diff LINE BY LINE (paths only).
            $expectedLines = Get-InventoryLines $invFile
            $actualLines = (Get-CanonicalInventory $path).Lines
            $expectedMap = ConvertTo-InventoryMap $expectedLines
            $actualMap = ConvertTo-InventoryMap $actualLines

            $onlyExpected = @($expectedMap.Keys | Where-Object { -not $actualMap.ContainsKey($_) } | Sort-Object)
            $onlyActual   = @($actualMap.Keys   | Where-Object { -not $expectedMap.ContainsKey($_) } | Sort-Object)
            $shaDiff      = @($expectedMap.Keys | Where-Object { $actualMap.ContainsKey($_) -and ($actualMap[$_] -ne $expectedMap[$_]) } | Sort-Object)

            if (($onlyExpected.Count -gt 0) -or ($onlyActual.Count -gt 0) -or ($shaDiff.Count -gt 0)) {
              Write-Host "${label}: expectedAggregate=$expectedAgg actualInventoryFileAggregate=$actualAgg"
              Write-Host "${label}: expectedLines=$($expectedLines.Count) actualLines=$($actualLines.Count)"
              Write-Host "${label}: onlyInExpected=$($onlyExpected.Count) onlyInActual=$($onlyActual.Count) shaDiffers=$($shaDiff.Count)"
              $diffs = @()
              foreach ($p in $onlyExpected) { $diffs += "ONLY-IN-EXPECTED: $p" }
              foreach ($p in $onlyActual)   { $diffs += "ONLY-IN-ACTUAL:   $p" }
              foreach ($p in $shaDiff)      { $diffs += "SHA-DIFF:         $p (expected=$($expectedMap[$p]) actual=$($actualMap[$p]))" }
              $diffs | Select-Object -First 20 | ForEach-Object { Write-Host $_ }
              throw "ARTIFACT INTEGRITY FAILURE ($label): downloaded files do not match the shipped canonical inventory. Aborting before touching the server."
            }
            Write-Host "${label}: integrity OK (aggregate=$expectedAgg, $($actualLines.Count) files)."
          }
          Confirm-Artifact "${{ env.ARTIFACT_API_PATH }}" "api"
          Confirm-Artifact "${{ env.ARTIFACT_WEB_PATH }}" "web"
          Write-Host "::notice::Artifact inventory + SHA-256 verified (api + web)."

      # --- Pre-Deploy Database Validation & Backup ---
      - name: Validate Production database name
        shell: pwsh
        run: |
          $connStr = "${{ secrets.PROD_DB_CONNECTION_STRING }}"
          if ([string]::IsNullOrWhiteSpace($connStr)) {
            Write-Host "::warning::PROD_DB_CONNECTION_STRING secret is not set. Skipping database validation."
            Write-Host "::warning::Database validation will rely on application startup."
            exit 0
          }

          # Diagnostic: detect common connection string issues before attempting to connect
          $connStrTrimmed = $connStr.Trim()
          if ($connStrTrimmed -ne $connStr) {
            Write-Host "::warning::PROD_DB_CONNECTION_STRING has leading/trailing whitespace. This may cause parsing failures."
            $connStr = $connStrTrimmed
          }
          if ($connStr -match '[\r\n]') {
            Write-Host "::error::PROD_DB_CONNECTION_STRING contains newline characters. GitHub secrets must be a single line."
            Write-Host "::error::Go to GitHub > Settings > Secrets > PROD_DB_CONNECTION_STRING and re-enter it as a single line."
            throw "Connection string contains newline characters — cannot parse."
          }

          try {
            $conn = New-Object System.Data.SqlClient.SqlConnection($connStr)
            $conn.Open()
            $cmd = $conn.CreateCommand()
            $cmd.CommandText = "SELECT DB_NAME() AS DatabaseName"
            $reader = $cmd.ExecuteReader()

            if ($reader.Read()) {
              $dbName = $reader["DatabaseName"]
              Write-Host "::notice::Connected to database: [$dbName]"

              if ($dbName -eq "Portal-Gerencial-Test") {
                $reader.Close()
                $conn.Close()
                throw "CRITICAL SAFETY CHECK FAILED: Connection string resolves to TEST database [Portal-Gerencial-Test]. Production deployment BLOCKED."
              }

              if ($dbName -ne "Portal-Gerencial") {
                $reader.Close()
                $conn.Close()
                throw "SAFETY CHECK FAILED: Expected database [Portal-Gerencial], got [$dbName]. Production deployment BLOCKED."
              }

              Write-Host "::notice::Database validation PASSED: target is [Portal-Gerencial]"
            }

            $reader.Close()
            $conn.Close()
          } catch {
            if ($_.Exception.Message -match "SAFETY CHECK") {
              throw
            }
            # Provide actionable guidance for connection string parsing errors
            if ($_.Exception.Message -match "Format of the initialization string") {
              Write-Host "::error::The PROD_DB_CONNECTION_STRING secret could not be parsed by SqlConnection."
              Write-Host "::error::Common causes:"
              Write-Host "::error::  1. Password contains an unescaped semicolon (;) — wrap the password in single quotes in the connection string."
              Write-Host "::error::  2. The secret was pasted with line breaks or invisible Unicode characters."
              Write-Host "::error::  3. The secret has trailing whitespace or BOM characters."
              Write-Host "::error::Fix: Go to GitHub > Repository > Settings > Secrets and re-enter PROD_DB_CONNECTION_STRING as a single, clean line."
            }
            Write-Host "::warning::Database connection test failed: $_"
            Write-Host "::warning::Deployment will continue — the application will validate on startup."
          }

      # --- GATE: backup destination usable + sufficient free space (fail-closed) ---
      # Run #46 class: the deploy was cancelled mid "Create timestamped file backup" with the
      # backup drive at 61 MB free. This gate resolves the CONFIGURED backup root
      # (vars.BACKUP_PATH — deliberately NO fallback to any hard-coded drive: unset variable or
      # unusable destination fails the deploy here, BEFORE the SQL backup, IIS or file steps),
      # proves the runner account can write to it, and requires enough free space. Required space
      # = current api+web deployment size × 1.2 (robocopy copies exactly those trees; the 20%
      # margin covers filesystem overhead and growth between check and copy) + the size of the
      # newest existing .bak × 1.1 as the SQL-backup allowance when history exists.
      - name: Validate backup destination and free space
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $backupRoot = "${{ vars.BACKUP_PATH }}"
          if ([string]::IsNullOrWhiteSpace($backupRoot)) {
            throw "BACKUP_PATH environment variable is not set for PRODUCTION. Fail-closed: there is no fallback backup drive. Configure it (e.g. D:\Backups\AlplaPortal\Prod) before deploying."
          }
          New-Item -ItemType Directory -Force -Path $backupRoot | Out-Null

          # Write probe: prove the runner account can create and remove files at the destination.
          $probe = Join-Path $backupRoot ("write-probe-" + [Guid]::NewGuid().ToString("N") + ".tmp")
          Set-Content -Path $probe -Value "probe" -ErrorAction Stop
          Remove-Item -Path $probe -Force -ErrorAction Stop

          $driveRoot = [System.IO.Path]::GetPathRoot((Resolve-Path -LiteralPath $backupRoot).Path)
          $drive = New-Object System.IO.DriveInfo($driveRoot)
          $systemRoot = [System.IO.Path]::GetPathRoot($env:SystemRoot)
          if ($driveRoot -ieq $systemRoot) {
            Write-Host "::warning::Backup destination '$backupRoot' is on the SYSTEM drive ($systemRoot). Point BACKUP_PATH at a data drive (e.g. D:) so backups can never exhaust the OS disk."
          }

          function Get-TreeSizeBytes([string]$path) {
            if (-not (Test-Path -LiteralPath $path)) { return [long]0 }
            $sum = (Get-ChildItem -LiteralPath $path -Recurse -File -Force -ErrorAction SilentlyContinue | Measure-Object -Property Length -Sum).Sum
            if ($null -eq $sum) { return [long]0 } else { return [long]$sum }
          }
          $appBytes = (Get-TreeSizeBytes "${{ vars.API_DEPLOY_PATH }}") + (Get-TreeSizeBytes "${{ vars.WEB_DEPLOY_PATH }}")

          # SQL-backup allowance: newest existing .bak is the best local estimator of the next one.
          $dbAllowanceBytes = [long]0
          $dbDir = Join-Path $backupRoot "db"
          if (Test-Path -LiteralPath $dbDir) {
            $latestBak = Get-ChildItem -LiteralPath $dbDir -Filter '*.bak' -File -ErrorAction SilentlyContinue | Sort-Object LastWriteTimeUtc -Descending | Select-Object -First 1
            if ($latestBak) { $dbAllowanceBytes = [long]($latestBak.Length * 1.1) }
          }
          if ($dbAllowanceBytes -eq 0) { Write-Host "No prior .bak found - SQL-backup allowance not estimable; file-backup requirement applies alone." }

          $requiredBytes = [long]($appBytes * 1.2) + $dbAllowanceBytes
          Write-Host ("Backup root: {0} (drive {1})" -f $backupRoot, $driveRoot)
          Write-Host ("Drive total: {0:N1} GB, free: {1:N1} GB" -f ($drive.TotalSize/1GB), ($drive.AvailableFreeSpace/1GB))
          Write-Host ("App size (api+web): {0:N2} GB; SQL allowance: {1:N2} GB; required with margin: {2:N2} GB" -f ($appBytes/1GB), ($dbAllowanceBytes/1GB), ($requiredBytes/1GB))
          if ($drive.AvailableFreeSpace -lt $requiredBytes) {
            throw ("INSUFFICIENT BACKUP SPACE - failing closed before any server mutation. Backup path: {0}. Required: {1:N2} GB. Available: {2:N2} GB on {3}. Free space on that drive or repoint BACKUP_PATH." -f $backupRoot, ($requiredBytes/1GB), ($drive.AvailableFreeSpace/1GB), $driveRoot)
          }
          Write-Host "::notice::Backup destination validated (write permission OK, free space OK)."

      - name: Create SQL database backup
        id: sql_backup
        shell: pwsh
        run: |
          $connStr = "${{ secrets.PROD_DB_CONNECTION_STRING }}"
          if ([string]::IsNullOrWhiteSpace($connStr)) {
            Write-Host "::warning::PROD_DB_CONNECTION_STRING not set. Skipping SQL backup."
            exit 0
          }

          $backupDir = "${{ vars.BACKUP_PATH }}\db"
          if (-not (Test-Path $backupDir)) {
            New-Item -ItemType Directory -Force -Path $backupDir | Out-Null
          }

          $timestamp = Get-Date -Format "yyyyMMdd_HHmmss"
          $backupFile = Join-Path $backupDir "Portal-Gerencial_$timestamp.bak"

          try {
            $conn = New-Object System.Data.SqlClient.SqlConnection($connStr)
            $conn.Open()

            # Detect SQL Server edition to decide if COMPRESSION is supported
            $editionCmd = $conn.CreateCommand()
            $editionCmd.CommandText = "SELECT SERVERPROPERTY('Edition') AS Edition"
            $editionResult = $editionCmd.ExecuteScalar()
            $edition = [string]$editionResult
            Write-Host "::notice::SQL Server Edition: $edition"

            # Express Edition does not support BACKUP WITH COMPRESSION
            if ($edition -match 'Express') {
              $backupSql = "BACKUP DATABASE [Portal-Gerencial] TO DISK = '$backupFile' WITH FORMAT, NAME = 'Pre-Deploy Backup ${{ inputs.version }}'"
              Write-Host "::notice::Using backup WITHOUT compression (Express Edition detected)."
            } else {
              $backupSql = "BACKUP DATABASE [Portal-Gerencial] TO DISK = '$backupFile' WITH FORMAT, COMPRESSION, NAME = 'Pre-Deploy Backup ${{ inputs.version }}'"
              Write-Host "::notice::Using backup WITH compression."
            }

            $cmd = $conn.CreateCommand()
            $cmd.CommandTimeout = 300
            $cmd.CommandText = $backupSql
            $cmd.ExecuteNonQuery() | Out-Null
            $conn.Close()
            Write-Host "::notice::Database backup created: $backupFile"
            # Signal for retention: only a run that SECURED a new .bak may clean older ones.
            "db_backup_created=true" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          } catch {
            Write-Host "::warning::Database backup failed: $_"
            Write-Host "::warning::Deployment will continue. Manual backup recommended before verifying."
          }

      # --- Retention (approved policy) for deployment DB backups: runs ONLY when this run secured
      #     a new .bak. Keeps the newest 5 whitelisted backups; deletes only older-than-14-days
      #     beyond those. Best-effort: warnings only, never blocks. dev-export lives outside db\. ---
      - name: Retention cleanup (database backups)
        if: steps.sql_backup.outputs.db_backup_created == 'true'
        shell: pwsh
        run: |
          & "${{ github.workspace }}\scripts\deploy\backup-retention.ps1" `
            -BackupRoot "${{ vars.BACKUP_PATH }}\db" `
            -Mode Database

      # --- GATE: database readiness + pending migrations — BEFORE stopping pools / replacing files ---
      # Corrected activation order: a pending-migration failure aborts with the PREVIOUS version still
      # live (pools never stopped) — new code is never activated against an un-migrated schema.
      - name: Verify database migration readiness
        shell: pwsh
        run: |
          $connStr = "${{ secrets.PROD_DB_CONNECTION_STRING }}"
          if ([string]::IsNullOrWhiteSpace($connStr)) {
            $configFile = Join-Path "${{ vars.API_DEPLOY_PATH }}" "appsettings.Production.json"
            if (Test-Path $configFile) { $connStr = (Get-Content $configFile -Raw | ConvertFrom-Json).ConnectionStrings.DefaultConnection }
          }
          if ([string]::IsNullOrWhiteSpace($connStr)) { throw "No PRODUCTION connection string available — cannot verify migrations (set PROD_DB_CONNECTION_STRING)." }

          $migrationListFile = Join-Path "${{ env.ARTIFACT_API_PATH }}" "expected-migrations.txt"
          if (-not (Test-Path $migrationListFile)) { throw "expected-migrations.txt not found in artifact (DEC-139)." }
          $expected = Get-Content $migrationListFile | Where-Object { $_ -match '\S' } | ForEach-Object { $_.Trim() }

          $conn = New-Object System.Data.SqlClient.SqlConnection($connStr)
          $conn.Open()
          $dbName = (New-Object System.Data.SqlClient.SqlCommand("SELECT DB_NAME()", $conn)).ExecuteScalar()
          if ($dbName -eq "Portal-Gerencial-Test") { $conn.Close(); throw "CRITICAL SAFETY CHECK: connection resolves to TEST database. PRODUCTION deployment BLOCKED." }
          $cmd = $conn.CreateCommand(); $cmd.CommandText = "SELECT MigrationId FROM __EFMigrationsHistory ORDER BY MigrationId"
          $reader = $cmd.ExecuteReader(); $applied = @(); while ($reader.Read()) { $applied += $reader["MigrationId"].ToString() }; $reader.Close(); $conn.Close()

          $pending = @(); foreach ($m in $expected) { if ($applied -notcontains $m) { $pending += $m } }
          if ($pending.Count -gt 0) {
            foreach ($m in $pending) { Write-Host "::error::PENDING migration: $m" }
            throw "DEPLOYMENT BLOCKED: $($pending.Count) pending migration(s) in PRODUCTION [$dbName]. Run 'Apply PRODUCTION Migrations' first. The previous version remains live."
          }
          Write-Host "::notice::All $($expected.Count) expected migrations are applied in PRODUCTION [$dbName]."

      # --- Backup Current Files ---
      - name: Create timestamped file backup
        id: file_backup
        shell: pwsh
        run: |
          $timestamp = Get-Date -Format "yyyyMMdd_HHmmss"
          $backupDir = Join-Path "${{ vars.BACKUP_PATH }}" "backup_$timestamp"
          New-Item -ItemType Directory -Force -Path $backupDir | Out-Null

          Write-Host "::notice::Backup directory: $backupDir"

          # Backup current API folder
          $apiDeploy = "${{ vars.API_DEPLOY_PATH }}"
          if (Test-Path $apiDeploy) {
            $apiBackup = Join-Path $backupDir "api"
            Write-Host "Backing up API from $apiDeploy to $apiBackup"
            robocopy $apiDeploy $apiBackup /E /NFL /NDL /NJH /NJS /NC /NS /NP
            if ($LASTEXITCODE -gt 7) { throw "API backup failed with robocopy exit code $LASTEXITCODE" }
          } else {
            Write-Host "::warning::API deploy path does not exist yet. Skipping API backup."
          }

          # Backup current Web folder
          $webDeploy = "${{ vars.WEB_DEPLOY_PATH }}"
          if (Test-Path $webDeploy) {
            $webBackup = Join-Path $backupDir "web"
            Write-Host "Backing up Web from $webDeploy to $webBackup"
            robocopy $webDeploy $webBackup /E /NFL /NDL /NJH /NJS /NC /NS /NP
            if ($LASTEXITCODE -gt 7) { throw "Web backup failed with robocopy exit code $LASTEXITCODE" }
          } else {
            Write-Host "::warning::Web deploy path does not exist yet. Skipping Web backup."
          }

          # Completion marker: rollback tooling/operators must never trust a backup directory
          # without this file — a run that dies mid-copy leaves no marker behind.
          Set-Content -Path (Join-Path $backupDir 'backup-complete.marker') -Value ("completed {0:o}" -f (Get-Date).ToUniversalTime())
          "backup_dir_name=backup_$timestamp" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

          # Reset exit code so the step doesn't fail on robocopy success codes 1-7
          $global:LASTEXITCODE = 0

      # --- Retention (approved policy): AFTER the new backup is secured, keep the newest 5
      #     completed backups unconditionally and delete only completed backups outside that set
      #     older than 14 days. Best-effort: cleanup failures warn, never block the deploy. ---
      - name: Retention cleanup (application backups)
        shell: pwsh
        run: |
          & "${{ github.workspace }}\scripts\deploy\backup-retention.ps1" `
            -BackupRoot "${{ vars.BACKUP_PATH }}" `
            -Mode Application `
            -CurrentBackupName "${{ steps.file_backup.outputs.backup_dir_name }}"

      # --- Stop PRODUCTION IIS App Pools ---
      - name: Stop Production IIS App Pools
        shell: pwsh
        run: |
          Import-Module WebAdministration

          $apiPool = "${{ vars.IIS_API_APPPOOL }}"
          $webPool = "${{ vars.IIS_WEB_APPPOOL }}"

          foreach ($pool in @($apiPool, $webPool)) {
            $state = (Get-WebAppPoolState -Name $pool).Value
            if ($state -eq "Started") {
              Write-Host "Stopping App Pool: $pool"
              Stop-WebAppPool -Name $pool
            } else {
              Write-Host "App Pool '$pool' is already in state: $state"
            }
          }

          # Wait for pools to fully stop
          Start-Sleep -Seconds 5

          foreach ($pool in @($apiPool, $webPool)) {
            $state = (Get-WebAppPoolState -Name $pool).Value
            Write-Host "App Pool '$pool' state after stop: $state"
          }

      # --- Deploy API ---
      - name: Deploy API files
        shell: pwsh
        run: |
          $apiSource = "${{ env.ARTIFACT_API_PATH }}"
          $apiDeploy = "${{ vars.API_DEPLOY_PATH }}"

          # Ensure deploy directory exists
          if (-not (Test-Path $apiDeploy)) {
            New-Item -ItemType Directory -Force -Path $apiDeploy | Out-Null
          }

          # ---------------------------------------------------------------
          # Preserve environment-specific configuration files on the server.
          # These files contain server-side secrets and environment config
          # that must NOT be overwritten by the build artifact.
          # ---------------------------------------------------------------
          $configFilesToPreserve = @(
            "appsettings.Production.json",
            "appsettings.Staging.json",
            "appsettings.Test.json",
            "appsettings.Local.json"
          )

          $tempConfigDir = Join-Path $env:TEMP "AlplaPortal_ConfigBackup_Prod_API"
          if (Test-Path $tempConfigDir) { Remove-Item -Path $tempConfigDir -Recurse -Force }
          New-Item -ItemType Directory -Force -Path $tempConfigDir | Out-Null

          # Save existing config files
          $preservedFiles = @()
          foreach ($configFile in $configFilesToPreserve) {
            $sourcePath = Join-Path $apiDeploy $configFile
            if (Test-Path $sourcePath) {
              Copy-Item -Path $sourcePath -Destination $tempConfigDir -Force
              $preservedFiles += $configFile
              Write-Host "Preserved: $configFile"
            }
          }

          # Deploy new API files (overwrite existing, add new, but do NOT mirror/delete)
          Write-Host "Deploying API files from $apiSource to $apiDeploy"
          robocopy $apiSource $apiDeploy /E /IS /IT /NFL /NDL /NJH /NJS /NC /NS /NP
          if ($LASTEXITCODE -gt 7) { throw "API deployment failed with robocopy exit code $LASTEXITCODE" }

          # Restore preserved config files (overwrite whatever the artifact brought)
          foreach ($configFile in $preservedFiles) {
            $backupPath = Join-Path $tempConfigDir $configFile
            $targetPath = Join-Path $apiDeploy $configFile
            Copy-Item -Path $backupPath -Destination $targetPath -Force
            Write-Host "Restored: $configFile"
          }

          # Cleanup temp
          Remove-Item -Path $tempConfigDir -Recurse -Force -ErrorAction SilentlyContinue

          # Reset exit code
          $global:LASTEXITCODE = 0

      # --- Deploy Web ---
      - name: Deploy Web files
        shell: pwsh
        run: |
          $webSource = "${{ env.ARTIFACT_WEB_PATH }}"
          $webDeploy = "${{ vars.WEB_DEPLOY_PATH }}"

          # Ensure deploy directory exists
          if (-not (Test-Path $webDeploy)) {
            New-Item -ItemType Directory -Force -Path $webDeploy | Out-Null
          }

          # ---------------------------------------------------------------
          # Preserve the server-side web.config (PRODUCTION reverse proxy
          # targeting port 5002). The build artifact ships with port 5001
          # (Test). We must NEVER overwrite this file.
          # ---------------------------------------------------------------
          $serverWebConfig = Join-Path $webDeploy "web.config"
          $tempWebConfig = Join-Path $env:TEMP "AlplaPortal_Prod_WebConfig_Backup_web.config"
          $preserveWebConfig = $false

          if (Test-Path $serverWebConfig) {
            Copy-Item -Path $serverWebConfig -Destination $tempWebConfig -Force
            $preserveWebConfig = $true
            Write-Host "Preserved server-side Production web.config (port 5002)"
          }

          # Deploy new Web files (mirror = clean old hashed assets)
          Write-Host "Deploying Web files from $webSource to $webDeploy"
          robocopy $webSource $webDeploy /MIR /NFL /NDL /NJH /NJS /NC /NS /NP
          if ($LASTEXITCODE -gt 7) { throw "Web deployment failed with robocopy exit code $LASTEXITCODE" }

          # Restore preserved web.config (ALWAYS for Production)
          if ($preserveWebConfig -and (Test-Path $tempWebConfig)) {
            Copy-Item -Path $tempWebConfig -Destination $serverWebConfig -Force
            Write-Host "Restored server-side Production web.config (port 5002)"
            Remove-Item -Path $tempWebConfig -Force -ErrorAction SilentlyContinue
          } else {
            Write-Host "::warning::No server-side web.config was preserved. Ensure the Production web.config exists with port 5002."
          }

          # Reset exit code
          $global:LASTEXITCODE = 0

      # --- Ensure + verify web.config cache policy (fail-closed, before activation) ---
      # PROD preserves its own server-side web.config (port-5002 reverse proxy), so the cache <location>
      # blocks are PATCHED into it here (add/normalise, never removing existing rules), then validated.
      # The deploy fails before activation if the port-5002 proxy or any cache rule is missing — the PROD
      # cache policy can never be silently absent.
      - name: Ensure web.config cache policy
        shell: pwsh
        run: |
          $webConfig = Join-Path "${{ vars.WEB_DEPLOY_PATH }}" "web.config"
          if (-not (Test-Path $webConfig)) { throw "web.config not found at $webConfig - cannot ensure cache policy." }
          [xml]$xml = Get-Content $webConfig
          $config = $xml.DocumentElement
          function Set-CacheLocation($xml, $config, $path, $value) {
            $existing = @($config.location) | Where-Object { $_.path -eq $path }
            if ($existing) {
              $add = @($existing.'system.webServer'.httpProtocol.customHeaders.add) | Where-Object { $_.name -eq 'Cache-Control' } | Select-Object -First 1
              if ($add) { $add.value = $value; return }
            }
            $loc = $xml.CreateElement('location'); $loc.SetAttribute('path', $path)
            $sws = $xml.CreateElement('system.webServer')
            $http = $xml.CreateElement('httpProtocol')
            $ch = $xml.CreateElement('customHeaders')
            $rem = $xml.CreateElement('remove'); $rem.SetAttribute('name','Cache-Control'); [void]$ch.AppendChild($rem)
            $addN = $xml.CreateElement('add'); $addN.SetAttribute('name','Cache-Control'); $addN.SetAttribute('value',$value); [void]$ch.AppendChild($addN)
            [void]$http.AppendChild($ch); [void]$sws.AppendChild($http); [void]$loc.AppendChild($sws)
            [void]$config.AppendChild($loc)
          }
          Set-CacheLocation $xml $config 'index.html' 'no-cache, must-revalidate'
          Set-CacheLocation $xml $config 'build-metadata.json' 'no-cache, must-revalidate'
          Set-CacheLocation $xml $config 'assets' 'public, max-age=31536000, immutable'
          $xml.Save($webConfig)

          # Fail-closed validation: port-5002 proxy AND all three cache rules must be present.
          [xml]$check = Get-Content $webConfig
          $proxy = $check.SelectNodes("//rewrite/rules/rule/action[@type='Rewrite']") | Where-Object { $_.url -match 'localhost:5002' }
          if (-not $proxy) { throw "web.config no longer contains the PRODUCTION reverse-proxy rule to localhost:5002. Aborting." }
          $req = @{ 'index.html' = 'no-cache'; 'build-metadata.json' = 'no-cache'; 'assets' = 'immutable' }
          foreach ($p in $req.Keys) {
            $loc = @($check.configuration.location) | Where-Object { $_.path -eq $p }
            if (-not $loc) { throw "web.config missing cache <location path='$p'> after patch." }
            $add = @($loc.'system.webServer'.httpProtocol.customHeaders.add) | Where-Object { $_.name -eq 'Cache-Control' } | Select-Object -First 1
            if (-not $add -or ($add.value -notmatch [regex]::Escape($req[$p]))) { throw "web.config cache rule for '$p' missing/incorrect after patch." }
          }
          Write-Host "::notice::PROD web.config cache policy ensured; port-5002 proxy preserved."

      # --- Stamp deployment-time metadata into the deployed backend manifest ---
      # Migration readiness was already validated BEFORE the pools were stopped (gate above), so there
      # is no post-deploy migration check here — activation cannot proceed against an un-migrated schema.
      - name: Write deployment metadata
        shell: pwsh
        run: |
          $manifestPath = Join-Path "${{ vars.API_DEPLOY_PATH }}" "build-manifest.json"
          if (-not (Test-Path $manifestPath)) { throw "build-manifest.json not found in deployed API folder." }
          $m = Get-Content $manifestPath -Raw | ConvertFrom-Json
          $m | Add-Member -NotePropertyName environment -NotePropertyValue "PROD" -Force
          $m | Add-Member -NotePropertyName deploymentId -NotePropertyValue "${{ github.run_id }}-${{ github.run_attempt }}" -Force
          $m | Add-Member -NotePropertyName githubRunId -NotePropertyValue "${{ github.run_id }}" -Force
          $m | Add-Member -NotePropertyName githubRunNumber -NotePropertyValue "${{ github.run_number }}" -Force
          $m | Add-Member -NotePropertyName githubRunAttempt -NotePropertyValue "${{ github.run_attempt }}" -Force
          $m | Add-Member -NotePropertyName deploymentStartedAtUtc -NotePropertyValue ([DateTime]::UtcNow.ToString("o")) -Force
          $m | ConvertTo-Json -Depth 5 | Set-Content -Path $manifestPath -Encoding UTF8
          Write-Host "::notice::Deployment metadata written (deploymentId=${{ github.run_id }}-${{ github.run_attempt }})"

      # --- Start PRODUCTION IIS App Pools (ACTIVATION) ---
      # Conditional on all prior gates (if: success()) — never if: always(). New code must not be
      # activated after a failed migration/metadata/deploy gate. Availability recovery is separate.
      - name: Start Production IIS App Pools
        if: success()
        shell: pwsh
        run: |
          Import-Module WebAdministration

          $apiPool = "${{ vars.IIS_API_APPPOOL }}"
          $webPool = "${{ vars.IIS_WEB_APPPOOL }}"

          foreach ($pool in @($apiPool, $webPool)) {
            try {
              $state = (Get-WebAppPoolState -Name $pool).Value
              if ($state -ne "Started") {
                Write-Host "Starting App Pool: $pool"
                Start-WebAppPool -Name $pool
              } else {
                Write-Host "App Pool '$pool' is already Started"
              }
            } catch {
              Write-Host "::error::Failed to start App Pool '$pool': $_"
            }
          }

          # Verify final state
          Start-Sleep -Seconds 3
          foreach ($pool in @($apiPool, $webPool)) {
            try {
              $state = (Get-WebAppPoolState -Name $pool).Value
              Write-Host "App Pool '$pool' final state: $state"
            } catch {
              Write-Host "::error::Could not read state for App Pool '$pool': $_"
            }
          }

      # --- Smoke Test ---
      - name: Smoke test - API health check
        shell: pwsh
        run: |
          # Wait for IIS worker processes to initialize and migrations to run
          Start-Sleep -Seconds 15

          $healthUrl = "${{ vars.PROD_API_HEALTH_URL }}"
          Write-Host "Smoke test URL: $healthUrl"

          try {
            $response = Invoke-WebRequest -Uri $healthUrl -UseBasicParsing -TimeoutSec 60
            $statusCode = $response.StatusCode

            if ($statusCode -eq 200) {
              Write-Host "::notice::Smoke test PASSED (HTTP $statusCode)"
              Write-Host "Response length: $($response.Content.Length) bytes"
            } else {
              Write-Host "::warning::Smoke test returned unexpected status code: $statusCode"
            }
          } catch {
            Write-Host "::error::Smoke test FAILED: $_"
            Write-Host "::error::The API may need additional time to start, or a migration may have failed."
            Write-Host "::error::Check the application event log on AOVIA1VMS011 for startup errors."
            Write-Host "::error::Health URL: $healthUrl"
            throw "Smoke test failed — check server logs for database migration or startup errors."
          }

      # --- Verify running build identity (API + frontend == expected) ---
      - name: Verify running build identity
        shell: pwsh
        run: |
          $expected = "${{ needs.build.outputs.build_id }}"
          $portal = "${{ vars.PROD_PORTAL_URL }}"
          if ([string]::IsNullOrWhiteSpace($portal)) {
            $h = "${{ vars.PROD_API_HEALTH_URL }}"
            if ($h -match '^(https?://[^/]+)') { $b = $Matches[1] } else { $b = "" }
            if ($b -and ($b -notmatch 'localhost')) { $portal = $b }
          }
          if ([string]::IsNullOrWhiteSpace($portal)) {
            throw "Cannot verify build identity: set repository variable PROD_PORTAL_URL (e.g. https://portalgerencial.alpla.net)."
          }
          $portal = $portal.TrimEnd('/')
          Start-Sleep -Seconds 3

          $apiVer = Invoke-RestMethod -Uri "$portal/api/app/version" -TimeoutSec 30
          Write-Host "API   : buildId=$($apiVer.buildId) status=$($apiVer.buildMetadataStatus)"
          if ($apiVer.buildMetadataStatus -ne 'Valid') { throw "API build metadata status is '$($apiVer.buildMetadataStatus)' (expected Valid)." }
          if ($apiVer.buildId -ne $expected) { throw "API buildId '$($apiVer.buildId)' != expected '$expected'." }

          $fe = Invoke-RestMethod -Uri "$portal/build-metadata.json" -Headers @{ 'Cache-Control' = 'no-cache' } -TimeoutSec 30
          Write-Host "Front : buildId=$($fe.buildId)"
          if ($fe.buildId -ne $expected) { throw "Frontend buildId '$($fe.buildId)' != expected '$expected'." }
          if ($fe.buildId -ne $apiVer.buildId) { throw "Frontend/API buildId mismatch." }

          Write-Host "::notice::Live build identity verified (API == frontend == $expected)."

      # --- Availability recovery (failure only) ---
      # Activation is gated on success(); if a step AFTER the pools were stopped failed, restore
      # availability. Safe because migration readiness was validated BEFORE the pools were stopped.
      - name: Restore Production IIS App Pools (availability recovery)
        if: failure() || cancelled()
        shell: pwsh
        run: |
          Import-Module WebAdministration
          foreach ($pool in @("${{ vars.IIS_API_APPPOOL }}", "${{ vars.IIS_WEB_APPPOOL }}")) {
            try {
              if ((Get-WebAppPoolState -Name $pool).Value -ne "Started") {
                Write-Host "Recovery: starting App Pool '$pool'"
                Start-WebAppPool -Name $pool
              }
            } catch { Write-Host "::error::Recovery failed to start App Pool '$pool': $_" }
          }

      # --- Verify Test Environment Not Impacted ---
      - name: Verify Test environment integrity
        if: always()
        shell: pwsh
        run: |
          Import-Module WebAdministration

          # Check Test App Pools are still running
          $testPools = @("AlplaPortal-Test-Api-Pool", "AlplaPortal-Test-Web-Pool")
          foreach ($pool in $testPools) {
            try {
              $state = (Get-WebAppPoolState -Name $pool).Value
              Write-Host "Test pool '$pool' state: $state"
              if ($state -ne "Started") {
                Write-Host "::warning::Test pool '$pool' is not running! State: $state"
              }
            } catch {
              Write-Host "::warning::Could not check Test pool '$pool': $_"
            }
          }

      # --- Summary ---
      - name: Deployment summary
        if: always()
        shell: pwsh
        run: |
          Write-Host "============================================="
          Write-Host "  PRODUCTION Deployment Summary"
          Write-Host "============================================="
          Write-Host "Version:      ${{ inputs.version }}"
          Write-Host "Environment:  PRODUCTION"
          Write-Host "API Path:     ${{ vars.API_DEPLOY_PATH }}"
          Write-Host "Web Path:     ${{ vars.WEB_DEPLOY_PATH }}"
          Write-Host "API Pool:     ${{ vars.IIS_API_APPPOOL }}"
          Write-Host "Web Pool:     ${{ vars.IIS_WEB_APPPOOL }}"
          Write-Host "Health URL:   ${{ vars.PROD_API_HEALTH_URL }}"
          Write-Host "Database:     [Portal-Gerencial]"
          Write-Host "API Port:     5002"
          Write-Host "============================================="
          Write-Host ""
          Write-Host "If pending migrations are detected, this workflow will BLOCK"
          Write-Host "startup. Run 'Apply PRODUCTION Migrations' workflow first."
          Write-Host "See: docs/DEPLOYMENT_CHECKLIST.md"
          Write-Host "============================================="
