/**
 * Centralized feature flags for the frontend application.
 */

export const features = {
    /**
     * Enables the new Quotation Wizard in the Buyer workspace.
     * When false, the legacy inline quotation flow is used.
     */
    ENABLE_QUOTATION_WIZARD: true,
};
